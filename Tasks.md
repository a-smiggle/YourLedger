# TASKS

1. Wire UserData and BankData into local storage so the seeded stores become the real persisted app state.
2. Replace the remaining demoProfile and scenarioSummaries consumers with reads from UserData.
3. Add merge logic that applies BankData overrides on top of loaded bank products before calculations run.